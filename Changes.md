# jQuery SelectUnique Changelog

## v0.1.0    2013-03-XX
* Fix `refresh` method, it didn't work before
* `selected` and `remaining` methods now return an array of `HTMLOptionElement`s
* Raise an error if an unknown method is called
